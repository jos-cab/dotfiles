# Kitty Terminal Configuration

# Main Configuration File
# This file includes all modular configuration files
# Edit individual files for specific settings

include fonts.conf
include cursor.conf
include scrollback.conf
include mouse.conf
include colors.conf
include window.conf
include keybindings.conf
include performance.conf

# Custom overrides
# Add any custom settings below that override the included configs

# Example: Override font size for this specific setup
# font_size 14.0

# Example: Custom keybinding
# map ctrl+shift+z toggle_layout stack
